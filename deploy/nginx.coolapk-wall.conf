server {
    listen 80;
    server_name your-domain.com;

    root /var/www/coolapk-wall;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /data/ {
        add_header Cache-Control "no-store";
    }
}
